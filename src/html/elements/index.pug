extends ../../_component/layout/template

block cool-config
    -
        var myTitle = 'Learn to Code';
        var myHeading = 'Learn to code HTML Elements for beginners';
        var myDescription = 'An HTML element usually consists of a start tag and an end tag, with the content inserted in between.';
        var landingPage = 'HTML';
        var activeLink = '/html/elements';

block awesome-content          
    h1=myHeading
    p=myDescription
    h2 Example
    h3 Not my first heading.
    p Not my first paragraph. :)#[br]And we make use of the break tag and #[span(style="color:blue") the span tag.]